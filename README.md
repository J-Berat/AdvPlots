# AdvPlots (clean)

**How to run (IMPORTANT):**
```bash
cd AdvPlots
julia --project=. deps.jl
julia --project=. -e 'import Pkg; Pkg.test()'
julia --project=. demo/demo.jl
```

- No `test/Project.toml` (tests use package extras).
- Package UUID: `4b07f121-2307-42c0-8344-6814ace90e04`
